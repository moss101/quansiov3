suite_id: eval_data_analysis
cases:
  - case_id: permitted_resolves
    task: analyze the spreadsheet and summarize these metrics
    snapshot:
      tools: [fs.read, artifact.create, artifact.update]
      capabilities: [read.internal, artifact.write]
    expect: resolve
  - case_id: missing_tool_excluded
    task: analyze the spreadsheet
    snapshot:
      tools: [fs.read]
      capabilities: [read.internal]
    expect: exclude
