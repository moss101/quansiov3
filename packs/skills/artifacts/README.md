# artifacts skill pack

**Canonical owner:** `packs/skills/artifacts` (CAP-003). Governed skill for
creating and editing versioned document, spreadsheet, slide, media and code
artifacts. Tool needs are `artifact.create` and `artifact.update`; the skill
cannot grant those tools. Fixtures are the round-trip sources the evals pin.
