# disaster recovery

**Canonical owner:** `infra/dr` (OPS-005). PostgreSQL PITR, object-store
versioning and restore drills for RecoveryConsistencyPoint. Restore is to a
captured event sequence + artifact manifest + effect settlement watermark.
