# quansio-contracts

**Canonical owner:** `crates/contracts` — generated Rust bindings for Quansio contracts.

Generated from `schemas/` by GOV-004 tooling. **Never hand edit**: CI regenerates
and fails on any diff. The single source of truth for contract shapes is
`schemas/`; the naming and shape authority is `DOMAIN.md`.

Build: `cargo test -p quansio-contracts`
