[package]
name = "quansio-events"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "RuntimeEvent store, transactional outbox and resumable projections."

[lints]
workspace = true

[dependencies]
quansio-core = { path = "../core" }
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "tls-rustls", "postgres", "json", "chrono"] }
thiserror = "2"

[dev-dependencies]
quansio-server = { path = "../server" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
